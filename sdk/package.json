{
  "name": "@basira-ai/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for the Basira on-chain trust layer — register agents, compose policy rule lists, submit and execute policy-gated intents on Solana.",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "keywords": ["solana", "ai-agent", "basira", "policy", "trust", "anchor"],
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/idl && cp src/idl/basira.json dist/idl/basira.json",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@solana/web3.js": "^1.98.0"
  }
}
